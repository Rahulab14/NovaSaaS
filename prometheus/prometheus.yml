global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alert_rules.yml

scrape_configs:
  - job_name: "novasaas-app"

    static_configs:
      - targets:
          - app:3000

alerting:
  alertmanagers:
    - static_configs:
        - targets:
            - alertmanager:9093